{
  "name": "snelstart-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "zip": "npm run build && cd dist && zip -r ../snelstart-extension.zip . && cd .."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.6"
  }
}
